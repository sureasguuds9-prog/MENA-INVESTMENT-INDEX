services:
  - type: web
    name: mena-investment-index
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m gunicorn wsgi:server --workers 2 --timeout 120 --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
    plan: free
    healthCheckPath: /
